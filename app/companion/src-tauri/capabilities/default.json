{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. Native dialog access is limited to opening a path; import remains read-only.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
