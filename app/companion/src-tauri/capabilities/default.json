{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core APIs only. The scaffold registers a single read-only command, so no additional permissions are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
